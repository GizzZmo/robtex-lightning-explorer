# Deprecated: image publish moved to cd.yml (CI/CD pipeline).
# Kept as a thin alias so existing badges/links keep working.
name: Docker

on:
  workflow_dispatch:

jobs:
  redirect:
    name: Use CD workflow
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "Docker image publishing is handled by the CD workflow."
          echo "Trigger: Actions → CD → Run workflow"
          echo "Or push to main / tag v*."
